// ============================================================
// FIREBASE CONFIG
// Copy this file to "firebase-config.js" (same folder) and paste
// in your real values from Firebase Console → Project settings →
// General → Your apps → SDK setup snippet.
// firebase-config.js itself is gitignored, so your real keys never
// get committed — only this placeholder template does.
// ============================================================
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
